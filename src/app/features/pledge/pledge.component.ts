import { Component } from '@angular/core';

/**
 * Pledge component
 * @class PledgeComponent
 */
@Component({
  selector: 'app-pledge',
  standalone: true,
  template: `<h1>Pledge Component</h1>`, // Temporary template
  styles: [''], // Will add styles later
})
export class PledgeComponent {}
