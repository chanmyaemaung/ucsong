import { Component } from '@angular/core';

/**
 * Song detail component
 * @class SongDetailComponent
 */
@Component({
  selector: 'app-song-detail',
  standalone: true,
  template: `<h1>Song Detail Component</h1>`, // Temporary template
  styles: [''], // Will add styles later
})
export class SongDetailComponent {}
