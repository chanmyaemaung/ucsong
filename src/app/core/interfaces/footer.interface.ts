/**
 * Footer translations interface
 * @interface IFooter
 */
export interface IFooter {
  credit: {
    my: string;
    en: string;
    th: string;
  };
  contact: {
    label: {
      my: string;
      en: string;
      th: string;
    };
    project: {
      my: string;
      en: string;
      th: string;
    };
    donate: {
      my: string;
      en: string;
      th: string;
    };
  };
}
